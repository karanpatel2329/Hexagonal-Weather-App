name: hexagonal_weather_app
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.4
  injectable: ^2.3.4
  get_it: ^7.6.7
  geolocator: ^9.0.2
  geocoding: ^2.1.0
  http: ^1.2.0
  lottie: ^3.1.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  injectable_generator: ^2.4.2
  build_runner: ^2.1.11
  
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/
    - .env


